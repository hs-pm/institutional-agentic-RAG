version: '3.8'

services:
  rag-api:
    build: .
    ports:
      - "8000:8000"
    volumes:
      - ./data:/app/data
      - ./faiss.index:/app/faiss.index
    environment:
      - DATA_DIR=/app/data
    restart: unless-stopped 